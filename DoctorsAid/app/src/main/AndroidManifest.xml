<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sujeeth.docaid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".page1Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".loginSuccess"
            android:parentActivityName=".page1Activity">
        </activity>
        <activity android:name=".patientInfo"
                  android:parentActivityName=".loginSuccess">
        </activity>
        <activity android:name=".addPatientInfo"
                  android:parentActivityName=".loginSuccess">
        </activity>
    </application>

</manifest>